import { Camera, Object3D, Vector3 } from 'three';
export declare function isFacingCamera(camera: Camera, object: Object3D, direction: Vector3, angle: number): boolean;
export declare function isAppleVisionPro(): boolean;
